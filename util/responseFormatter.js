/**
 * A response wrapper utility function to return responses with a uniform structure and format.
 * @param {Response} res 
 * @param {JSON} jsonData 
 * @param {String} msg 
 * @returns {JSON} 
 */
const responseFormatter = (res, jsonData, msg)=>{
    //Pending feature
}